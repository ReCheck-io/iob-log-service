# Basic Server Configuration
PORT=4000
NODE_ENV=development

# CORS Configuration
ALLOWED_ORIGINS=http://localhost:3000,https://your-frontend.com

# Certificate Mode Configuration
# Options: 'nginx' (production with nginx proxy) or 'direct' (development with local certs)
CERT_MODE=nginx

# Direct TLS Mode Configuration (when CERT_MODE=direct)
# These paths are used when Node.js handles TLS directly (development mode)
TLS_CERT_PATH=./certs/server.crt
TLS_KEY_PATH=./certs/server.key
TLS_CA_PATH=./certs/ca.crt

# ICP Canister Configuration
CANISTER_ID_IOB=your-canister-id-here

# Optional: Service Identity (auto-created if not provided)
# SERVICE_PRINCIPAL=./identity.secret

# ICP Network Configuration
ICP_NETWORK=local
ICP_HOST=http://localhost:4943

# Development Settings
# Set to false for development with self-signed certificates
# REJECT_UNAUTHORIZED=true 